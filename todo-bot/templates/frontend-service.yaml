apiVersion: v1
kind: Service
metadata:
  name: {{ include "todo-bot.fullname" . }}-frontend-service
  labels:
    {{- include "todo-bot.labels" . | nindent 4 }}
    app: frontend
spec:
  type: {{ .Values.frontend.service.type }}
  ports:
    - port: {{ .Values.frontend.service.port }}
      targetPort: {{ .Values.frontend.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "todo-bot.selectorLabels" . | nindent 4 }}
    app: frontend