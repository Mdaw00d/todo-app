apiVersion: v1
kind: Service
metadata:
  name: {{ include "todo-bot.fullname" . }}-backend-service
  labels:
    {{- include "todo-bot.labels" . | nindent 4 }}
    app: backend
spec:
  type: {{ .Values.backend.service.type }}
  ports:
    - port: {{ .Values.backend.service.port }}
      targetPort: {{ .Values.backend.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "todo-bot.selectorLabels" . | nindent 4 }}
    app: backend